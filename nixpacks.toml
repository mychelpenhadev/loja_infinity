providers = ["php"]

[variables]
NIXPACKS_PHP_EXTENSIONS = "zip pdo_mysql"

[phases.setup]
cmds = ["cp php.ini $NIXPACKS_PHP_INI_DIR/overrides.ini 2>/dev/null || true", "mkdir -p api/cache uploads/banners uploads/products uploads/profile_pics uploads/clientes", "chmod -R 777 uploads api/cache"]
